import React from 'react';
import images from '../constants/images';

const HeadingSec = ({title}) => {
  return (
    <div>
      <h1 className='main-title'>{title}</h1>
      
    </div>
  )
}

export default HeadingSec
